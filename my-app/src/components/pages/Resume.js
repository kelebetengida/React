import React from "react";

const Resume = () => {
    return (
        <div style={{display: "flex", justifyContent: "center"}}>
            <h1>Sign In</h1>
        </div>
        );
    
};

export default Resume;